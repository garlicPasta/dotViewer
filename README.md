# AndroidPointCloudVisualizer

Android Application for displaying a pointcloud(xyzrgb) streamed from a server.

Features so far:

* Allows to rotate the object
* Loads and displays models from disc and display then in color




